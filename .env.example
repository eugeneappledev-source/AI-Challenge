# Public address handled by Caddy. Use http://<VPS_IP> until a domain is ready.
SERVER_ADDRESS=http://176.53.173.246

# Backend settings
SERVER_PORT=8080
DEEPSEEK_API_URL=https://api.deepseek.com/chat/completions
DEEPSEEK_API_KEY=replace_with_your_key
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_SYSTEM_PROMPT=You are a helpful assistant. Answer clearly and concisely.

# Shared demo token. Generate a long random value and keep it out of Git.
APP_ACCESS_TOKEN=replace_with_a_long_random_value

# Public demo protection. One web comparison uses two requests.
RATE_LIMIT_PER_MINUTE=10
DAILY_REQUEST_LIMIT=200
